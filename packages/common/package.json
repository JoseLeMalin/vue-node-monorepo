{
  "name": "common",
  "author": "JoseLeMalin <seb_488@hotmail.fr>",
  "version": "1.0.0",
  "description": "",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc -p ./ts.config.json",
    "build:check": "tsc -p ./ts.config.json --noEmit",
    "build:watch": "tsc -p ./ts.config.json --watch",
    "test": "echo \"Error: no test specified\" && exit 1",
    "clean": "pnpm rm -rf dist"
  },
  "keywords": [],
  "license": "ISC",
  "dependencies": {
    "zod": "^3.22.0"
  },
  "types": "dist/index.d.ts"
}
